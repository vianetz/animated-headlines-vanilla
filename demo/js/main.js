document.addEventListener('DOMContentLoaded', function () {
    AnimatedHeadline('.animate-rotate-1 .ah-words-wrapper', {
        animationType: 'rotate-1'
    });

    AnimatedHeadline('.animate-type .ah-words-wrapper', {
        animationType: 'type'
    });

    AnimatedHeadline('.animate-rotate-2 .ah-words-wrapper', {
        animationType: 'rotate-2'
    });

    AnimatedHeadline('.animate-loading-bar .ah-words-wrapper', {
        animationType: 'loading-bar'
    });

    AnimatedHeadline('.animate-slide .ah-words-wrapper', {
        animationType: 'slide'
    });

    AnimatedHeadline('.animate-clip .ah-words-wrapper', {
        animationType: 'clip'
    });

    AnimatedHeadline('.animate-zoom .ah-words-wrapper', {
        animationType: 'zoom'
    });

    AnimatedHeadline('.animate-rotate-3 .ah-words-wrapper', {
        animationType: 'rotate-3'
    });

    AnimatedHeadline('.animate-scale .ah-words-wrapper', {
        animationType: 'scale'
    });

    AnimatedHeadline('.animate-push .ah-words-wrapper', {
        animationType: 'push'
    });
});